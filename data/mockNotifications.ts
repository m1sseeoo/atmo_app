/**
 * Legacy mock notifications. Persisted notifications load from AsyncStorage via notificationService.
 */
import type { AtmoNotification } from '../types/notification';

export const MOCK_NOTIFICATIONS: AtmoNotification[] = [];
