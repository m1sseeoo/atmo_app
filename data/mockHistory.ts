/**
 * Legacy mock history samples. Persisted history is loaded from AsyncStorage via historyService.
 */
import type { HistoryItem } from '../types/history';

export const MOCK_HISTORY: HistoryItem[] = [];
